// Initializng Variables
var signUpBar = document.querySelector(".signup-bar");
var signInBar = document.querySelector(".signin-bar");

// Initiailizing default display.
signUpBar.className += " active";
signInBar.className += " non-active";